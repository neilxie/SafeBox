package com.max.security.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.max.security.greendao.FileModels;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table FILE_MODELS.
*/
public class FileModelsDao extends AbstractDao<FileModels, String> {

    public static final String TABLENAME = "FILE_MODELS";

    /**
     * Properties of entity FileModels.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, String.class, "Id", true, "ID");
        public final static Property FileName = new Property(1, String.class, "FileName", false, "FILE_NAME");
        public final static Property FilePath = new Property(2, String.class, "FilePath", false, "FILE_PATH");
        public final static Property OriginPath = new Property(3, String.class, "OriginPath", false, "ORIGIN_PATH");
        public final static Property FileType = new Property(4, int.class, "FileType", false, "FILE_TYPE");
    };


    public FileModelsDao(DaoConfig config) {
        super(config);
    }
    
    public FileModelsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'FILE_MODELS' (" + //
                "'ID' TEXT PRIMARY KEY NOT NULL ," + // 0: Id
                "'FILE_NAME' TEXT NOT NULL ," + // 1: FileName
                "'FILE_PATH' TEXT NOT NULL ," + // 2: FilePath
                "'ORIGIN_PATH' TEXT NOT NULL ," + // 3: OriginPath
                "'FILE_TYPE' INTEGER NOT NULL );"); // 4: FileType
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'FILE_MODELS'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, FileModels entity) {
        stmt.clearBindings();
        stmt.bindString(1, entity.getId());
        stmt.bindString(2, entity.getFileName());
        stmt.bindString(3, entity.getFilePath());
        stmt.bindString(4, entity.getOriginPath());
        stmt.bindLong(5, entity.getFileType());
    }

    /** @inheritdoc */
    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.getString(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public FileModels readEntity(Cursor cursor, int offset) {
        FileModels entity = new FileModels( //
            cursor.getString(offset + 0), // Id
            cursor.getString(offset + 1), // FileName
            cursor.getString(offset + 2), // FilePath
            cursor.getString(offset + 3), // OriginPath
            cursor.getInt(offset + 4) // FileType
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, FileModels entity, int offset) {
        entity.setId(cursor.getString(offset + 0));
        entity.setFileName(cursor.getString(offset + 1));
        entity.setFilePath(cursor.getString(offset + 2));
        entity.setOriginPath(cursor.getString(offset + 3));
        entity.setFileType(cursor.getInt(offset + 4));
     }
    
    /** @inheritdoc */
    @Override
    protected String updateKeyAfterInsert(FileModels entity, long rowId) {
        return entity.getId();
    }
    
    /** @inheritdoc */
    @Override
    public String getKey(FileModels entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
